<h1>Your Concert Cal</h1>

<% @my_events.each do |event| %>
    <b><%= event.event.artist %></b><br>
    Venue: <%= event.event.venue %><br>
    Date: <%= event.event.date.rfc2822.slice(0, event.event.date.rfc2822.length-14) %><br>
    <hr>
<% end %>

